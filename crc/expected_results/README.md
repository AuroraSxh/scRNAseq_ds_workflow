# Expected Results: CRC Workflow

This workflow is expected to write structured results under:

```text
results/
├── qc/
├── integration/
├── annotation/
├── cd8/
├── macrophage/
├── communication/
└── bio-foundation-housekeeping/
```

The detailed intended organization is preserved in:

- `expected_results/results/README.md`

This repository does not include:
- integrated `.h5ad` objects
- figures
- tables
- CellChat outputs

It includes only the expected result layout for reproducibility planning.
